import styled from '@emotion/styled';

const Container = styled.div({
  height: 'calc(100vh - 60px)',
});

export const Form = () => {
  return (
    <Container>
      <form></form>
    </Container>
  );
};
