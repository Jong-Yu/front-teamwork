import styled from '@emotion/styled';

const Span = styled.span({
  fontSize: '1.2rem',
  fontWeight: '100',
  marginBottom: '1rem',
  textAlign: 'center',
});

export const SubTitle = () => {
  return <Span>편리한 동호회 운영을 경험해보세요</Span>;
};
