import styled from '@emotion/styled';

const Span = styled.span({
  fontFamily: "'Kanit', sans-serif",
  fontSize: '2.5rem',
  fontWeight: 'bold',
  marginBottom: '1rem',
  textAlign: 'center',
});

export const Title = () => {
  return <Span>Team Work</Span>;
};
